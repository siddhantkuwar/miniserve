"""Assignment 6 scaffold: inspect one real-model forward pass."""


# TODO: Print shape, dtype, and a tiny safe value summary after evaluation.
def summarize_array(name, array):
    """Print shape, dtype, and a tiny safe value summary after evaluation."""
    pass


# TODO: Show top candidate token IDs/text for the final prompt position.
def inspect_last_position_logits(logits, tokenizer, top_k):
    """Show top candidate token IDs/text for the final prompt position."""
    pass


# TODO: Load the adapter, encode one prompt, run once, and print the tensor contract.
def main():
    """Load the adapter, encode one prompt, run once, and print the tensor contract."""
    pass
