"""Assignment 9 scaffold: capture oracle tokens separately from MiniServe."""


# TODO: Apply exactly one declared prompt-format path for both implementations.
def build_reference_prompt(tokenizer, prompt, use_chat_template):
    """Apply exactly one declared prompt-format path for both implementations."""
    pass


# TODO: Use the allowed `mlx-lm` oracle path and return token IDs plus metadata.
def collect_reference_greedy_tokens(model, tokenizer, prompt, count):
    """Use the allowed `mlx-lm` oracle path and return token IDs plus metadata."""
    pass


# TODO: Persist model revision, runtime versions, prompt tokens, and output tokens.
def write_golden_fixture(path, record):
    """Persist model revision, runtime versions, prompt tokens, and output tokens."""
    pass


# TODO: Capture one explicit oracle fixture; never import this from runtime code.
def main():
    """Capture one explicit oracle fixture; never import this from runtime code."""
    pass
