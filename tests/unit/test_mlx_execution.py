"""Assignment 5 test prompts for framework and execution-model parity."""


# TODO: Declare dtype and tolerance explicitly; do not rely on defaults.
def test_torch_and_mlx_mlp_outputs_match_with_shared_parameters():
    """Declare dtype and tolerance explicitly; do not rely on defaults."""
    pass


# TODO: Compare values after both results are explicitly evaluated.
def test_cpu_and_gpu_stream_outputs_match():
    """Compare values after both results are explicitly evaluated."""
    pass


# TODO: Validate harness structure, not a speed threshold.
def test_timing_helper_returns_one_sample_per_repeat():
    """Validate harness structure, not a speed threshold."""
    pass


# TODO: Prevent accidental aggregation across different workloads.
def test_changing_shape_is_recorded_as_a_distinct_benchmark_case():
    """Prevent accidental aggregation across different workloads."""
    pass
