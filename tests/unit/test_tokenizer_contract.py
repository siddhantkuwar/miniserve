"""Assignment 3 test prompts for tokenizer contracts."""


# TODO: Choose text with an expected stable round trip and explain limitations.
def test_encode_decode_round_trip_for_simple_ascii_text():
    """Choose text with an expected stable round trip and explain limitations."""
    pass


# TODO: Use literal token IDs so the next-token relationship is unmistakable.
def test_shifted_labels_are_exactly_one_position_ahead():
    """Use literal token IDs so the next-token relationship is unmistakable."""
    pass


# TODO: Inspect control tokens; do not assume all models format chat identically.
def test_chat_template_generation_prompt_changes_suffix_only():
    """Inspect control tokens; do not assume all models format chat identically."""
    pass


# TODO: Termination should compare token IDs, not rendered token strings.
def test_eos_is_detectable_without_decoding_text():
    """Termination should compare token IDs, not rendered token strings."""
    pass
